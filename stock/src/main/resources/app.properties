## common
# if not given parameters for cli, then use cli.paras
cli.paras=

# whether using proxy, value is true | false
proxy.enable=true1
proxy.host=proxy.cmcc
proxy.port=8080
proxy.user=
proxy.password=

## spider
download.concurrent.num=1
# The directory where web pages download to. Warn: cannot use "~/"
download.directory=${user.home}/share/stock

# proxy pool
# whether using proxy, value is true | false
spider.proxies.file=proxies.conf
spider.proxies.enable=false

# mongo
mongo.host=mongomaster
mongo.port=27017
mongo.user=
mongo.password=
mongo.database=stock

# redis
redis.host=redismaster
redis.port=6379

# DB properties:
db.driverClass=com.mysql.jdbc.Driver
db.url=jdbc:mysql://localhost:23306/stock
db.username=snap
db.password=snap_cm
db.minPoolSize=10
db.maxPoolSize=100